---
const { title, description, delay } = Astro.props;
---

<div
  class="group p-6 rounded-xl border border-zinc-900 hover:border-zinc-800 transition-colors"
  style={{ animationDelay: `${delay}ms` }}
>
  <h3 class="text-lg font-medium mb-2 group-hover:text-white transition-colors">
    {title}
  </h3>
  <p class="text-zinc-400 leading-relaxed">{description}</p>
</div>
