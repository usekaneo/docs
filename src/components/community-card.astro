---
const { title, description, link, linkText } = Astro.props;
---

<div class="group p-6 rounded-xl border border-zinc-900 hover:border-zinc-800 transition-colors">
    <div class="text-zinc-500 group-hover:text-white transition-colors mb-4">
      <slot />
    </div>
    <h3 class="text-lg font-medium mb-2 group-hover:text-white transition-colors">
      {title}
    </h3>
    <p class="text-zinc-400 text-sm mb-4">
      {description}
    </p>
    <a 
      href={link}
      class="text-sm text-zinc-500 hover:text-white transition-colors inline-flex items-center gap-2"
    >
      {linkText} →
    </a>
  </div>
